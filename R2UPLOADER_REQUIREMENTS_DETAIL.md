# R2Uploader 需求详细说明

> 对应总览：[R2Uploader 可视化产品报告](./R2UPLOADER_VISUAL_PRODUCT_REPORT.svg)<br>
> 范围：P0–P3，共 58 项需求<br>
> 原则：先保证安全、真实、可恢复，再追求效率、差异化和个人生态扩张

## 阅读方式

每项需求都包含四部分：

- **用户问题**：为什么用户会需要它；
- **需求定义**：产品需要提供什么；
- **关键规则**：容易做错或必须提前确定的边界；
- **验收标准**：怎样判断它不是“按钮已经做出来”，而是真的可用。

优先级含义：

- **P0**：涉及公开暴露、Secret、数据一致性、错误认知或发布可信度；未完成不应发布下一版。
- **P1**：决定用户能否把产品当作日常素材管理器使用。
- **P2**：形成相对 Cloudflare Dashboard、CLI 和普通 S3 客户端的差异化。
- **P3**：个人效率与生态扩张，需要先由留存与实际需求证明。

# P0：信任与可靠性地基

## P0-01 Private-first bucket

- **用户问题**：用户打开 bucket 的意图通常只是查看内容，不代表同意把对象公开到互联网。当前应用在打开现有 bucket 和创建新 bucket 时会尝试开启 managed domain，阅读动作产生了权限副作用。
- **需求定义**：连接、打开和创建 bucket 时默认保持私有。公开访问必须成为独立、可见、需要明确确认的操作。
- **关键规则**：
  - bucket 列表要显示 `Private`、`Public via r2.dev`、`Public via custom domain` 等真实状态；
  - 开启 `r2.dev` 前解释其用途、限流和暴露范围；
  - 创建 bucket 时默认选择 Private，不能只用一行弱提示告知“默认公开”；
  - 浏览私有 bucket 不依赖任何公开域名。
- **验收标准**：自动化测试记录打开前后的 bucket 域名配置；完成浏览、上传和退出后，私有 bucket 的公开状态保持不变。

## P0-02 认证对象访问层

- **用户问题**：当前预览、查重、下载、复制和重命名依赖公开 URL。私有 bucket 即使能列出对象，也不能完整使用产品。
- **需求定义**：在 main process 建立统一的 R2 Object Service，通过经过认证的 S3/R2 API执行 HEAD、GET、PUT、DELETE、COPY 和 LIST。
- **关键规则**：
  - renderer 只能传 bucket、key、操作参数，不能自己拼远端 URL；
  - 私有预览使用受控的本地流、临时 URL 或安全代理；
  - 公开 URL 只属于“分享”能力，不属于内部读写基础设施；
  - 对象服务统一处理 key 编码、超时、重试和错误转换。
- **验收标准**：关闭 bucket 所有公开入口后，仍能完成浏览、图片预览、下载、上传、查重、重命名和删除。

## P0-03 系统安全凭据保险库

- **用户问题**：当前 Token 明文保存在用户目录，并会被完整返回 renderer。设备被共享、备份泄漏或 renderer 被攻击时，Secret 暴露面过大。
- **需求定义**：使用 macOS Keychain/Windows Credential Manager，或 Electron `safeStorage` 加密 Secret；Account ID 等非敏感信息可以单独保存。
- **关键规则**：
  - Secret 只在 main process 内解密和使用；
  - UI 只知道“已配置”“最后四位”或凭据别名；
  - 输入框默认遮蔽，并提供短暂显示按钮；
  - 首次升级要迁移旧 `r2uploader.json`，迁移成功后清除明文；
  - 日志、错误、诊断包和崩溃报告都必须脱敏。
- **验收标准**：扫描磁盘、renderer state、IPC payload 和日志，均找不到完整 Token；旧版本用户升级后无需重新配置。

## P0-04 连接测试与权限诊断

- **用户问题**：用户不知道应该使用哪一种 Token，也无法分辨 Account ID 错误、Token 无效、权限不足或某个 bucket 不在授权范围。
- **需求定义**：把“保存凭据”和“连接测试”拆开，提供逐步诊断结果。
- **关键规则**：
  - 分别测试认证、列 bucket、读取对象和写入权限；
  - 写权限测试不能在真实目录留下垃圾对象，可使用专用临时 key 并清理；
  - 显示最小权限建议，不默认要求全账号 Admin；
  - 失败信息要包含可操作建议和官方配置入口。
- **验收标准**：使用错误 Account ID、过期 Token、只读 Token、单 bucket Token 和网络断开分别测试，UI 能准确区分并给出下一步。

## P0-05 统一错误模型

- **用户问题**：当前多个失败路径最终表现为“没有 bucket”“没有对象”或 `Upload failed`，用户会误以为远端数据不存在。
- **需求定义**：建立跨 main、IPC 和 renderer 的 typed error，包括认证、权限、网络、超时、限流、冲突、不存在、部分成功和未知服务错误。
- **关键规则**：
  - 真实空数据和加载失败必须使用完全不同的页面状态；
  - 错误保留 Cloudflare code、HTTP status 和 request id，但不暴露 Secret；
  - 可重试错误提供 Retry，不可重试错误提供修复入口；
  - Toast 只做轻反馈，阻断性错误要保留在页面上，不能一闪而过。
- **验收标准**：故障注入覆盖主要错误类型；任何非成功响应都不会落入空状态；用户可以复制脱敏错误详情。

## P0-06 认证式对象查重

- **用户问题**：通过公开 URL 发 HEAD 会受到 bucket 私有状态、CDN 缓存、自定义域名配置和访问控制影响，可能把已存在判断成不存在，或反过来。
- **需求定义**：使用认证对象 API 查询目标 key 的元数据，在上传、粘贴、创建目录和重命名前复用同一套检查能力。
- **关键规则**：
  - 冲突对话框显示现有对象大小、修改时间和目标路径；
  - 提供跳过、覆盖、自动改名、应用到全部四种选择；
  - 大批量上传要做受控并发，不能为每个文件无限并行请求；
  - 如果无法确认是否存在，不能默认当作不存在。
- **验收标准**：私有 bucket、带缓存的自定义域名和不存在公开域名时，查重结果仍与远端对象状态一致。

## P0-07 原子化重命名与移动

- **用户问题**：对象存储的“改名”通常是复制后删除。当前实现没有完整验证删除结果，上传对话框中的改名还可能先删除旧对象，造成数据丢失。
- **需求定义**：把重命名和移动实现成可恢复事务：创建目标、验证目标、删除来源、再次确认最终状态。
- **关键规则**：
  - 目标上传或复制成功以前绝不删除来源；
  - 目标存在时先进入冲突流程；
  - 删除来源失败时显示“已复制，原文件仍保留”，而不是假装完全成功；
  - 应用重启后可以继续处理未完成事务；
  - UI 只在远端复核后更新。
- **验收标准**：在复制中断、校验失败、删除失败和应用退出等故障下，原对象都不会丢失，并且用户能看到和修复部分成功状态。

## P0-08 可靠上传引擎

- **用户问题**：当前虽然显示上传状态，但没有真实进度、取消、并发限制和大文件策略；界面声称 300MB 限制，代码并未执行。
- **需求定义**：建立独立 Transfer Manager，负责单文件、批量文件、粘贴和文件夹上传。
- **关键规则**：
  - 显示每个文件与总体进度、速度和剩余时间；
  - 支持取消、重试和有限并发；
  - 大文件使用 multipart，并清理未完成 upload；
  - 明确支持的实际大小，不显示无法兑现的限制；
  - 上传结束后通过 HEAD/metadata 验证 key、size 和必要校验信息。
- **验收标准**：小文件、300MB 以上文件、网络中断、批量冲突、取消和重试均有端到端测试；完成状态必须与远端一致。

## P0-09 安全删除与恢复

- **用户问题**：删除是不可逆操作，但当前确认信息不精确，bucket 删除文案与真实 API 行为不一致，也没有统一恢复策略。
- **需求定义**：为对象、文件夹、批量对象和 bucket 分别设计删除流程，默认优先提供可恢复路径。
- **关键规则**：
  - 对象可先移动到约定的 Trash prefix 或进入备份策略；
  - 永久删除显示对象数量、总体积、路径和不可恢复后果；
  - 删除 bucket 明确要求 bucket 为空，不能声称会连内容一起删除；
  - 部分删除失败时列出失败项，不能只显示一个总结果。
- **验收标准**：误删对象可从产品内恢复；永久删除需要显式确认；批量删除的远端结果逐项可核对。

## P0-10 可见的访问状态

- **用户问题**：用户看到一个 URL 时，不知道它是永久公开、临时授权、仅本机可用还是根本不可访问。
- **需求定义**：在 bucket、对象详情和复制分享入口持续显示访问状态。
- **关键规则**：
  - 至少区分 Private、Public r2.dev、Public Custom Domain、Temporary Link；
  - 公开状态改变时说明影响范围；
  - 临时链接显示过期时间和剩余有效期；
  - 不能用同一个 Copy 图标混合复制 key、内部 URL 和公开分享链接。
- **验收标准**：用户不进入 Cloudflare Dashboard，也能准确回答当前对象谁可以访问、通过什么入口访问、何时失效。

## P0-11 修复已确认可用性缺陷

- **用户问题**：公开 Issues 已出现有对象却显示为空、文件夹全部在单行被裁切、窗口拖动困难等问题。这些问题直接阻断基础使用。
- **需求定义**：建立针对 Issues #2、#3、#5 的明确修复与回归测试，而不是只修当前截图对应的表面现象。
- **关键规则**：
  - 文件夹-only bucket 不能被误判为空；
  - 文件夹网格随窗口宽度换行或虚拟滚动；
  - 标题栏可拖动区域不能被交互控件完全占用；
  - 失败响应必须进入错误状态，而不是 Empty State。
- **验收标准**：把 Issue 中描述的环境和数据结构写成回归场景；macOS 与 Windows 均通过。

## P0-12 双平台发布闭环

- **用户问题**：当前源码比最新 Release 多 29 个提交，但仍使用相同版本号；Windows 下载还指向旧版本。用户无法判断自己是否拿到修复。
- **需求定义**：建立从 commit、版本、构建、签名、发布到自动更新的唯一链路。
- **关键规则**：
  - 发布 tag、`package.json`、应用 About、artifact 名称和更新 manifest 使用同一版本；
  - macOS arm64、macOS x64、Windows 来自同一 commit；
  - macOS 完成 codesign 与 notarization；Windows 完成签名；
  - Release Notes 明确用户可感知变化和迁移风险；
  - 更新失败可重试，保留手动下载入口。
- **验收标准**：从上一公开版本实际执行自动升级；安装后核对版本和 commit；三个目标平台产物均可安装启动。

## P0-13 License 与开源策略

- **用户问题**：README 同时表达 GPL、需要购买 License、又可以免费使用，而应用实际会在未激活时阻止进入。用户无法理解付费价值，也会怀疑开源承诺。
- **需求定义**：先确定商业模式，再让代码、README、购买页、应用内文案和支持政策一致。
- **关键规则**：
  - 明确免费核心、高级功能、官方二进制、自动更新和技术支持分别如何授权；
  - 激活失败不能误伤已合法使用的用户；
  - 离线、License 服务故障和换机要有明确策略；
  - 不用模糊文案回避 GPL 与商业发行的关系。
- **验收标准**：新用户阅读一页说明后能回答“是否必须付费、付费买什么、源码自行构建有什么不同”；产品行为与答案一致。

## P0-14 Electron 安全基线

- **用户问题**：renderer 会加载远程对象内容，同时 preload 暴露通用 IPC；一旦出现内容注入，攻击面会延伸到打开外部链接、文件下载、缓存删除和配置读取。
- **需求定义**：收窄 renderer 能调用的能力，把每个 IPC 变成显式、类型化、参数可验证的 command。
- **关键规则**：
  - channel allowlist，不暴露通用 `invoke(channel, payload)`；
  - 所有参数使用 schema 验证；
  - 外部 URL 限制协议和 host；本地路径限制到应用管理目录或用户显式选择；
  - 设置 CSP、`setWindowOpenHandler`、导航拦截并评估 Electron sandbox；
  - 远程图片、视频、PDF 和文本使用隔离预览策略。
- **验收标准**：安全测试证明 renderer 无法读取 Secret、删除任意本地文件、打开任意协议或调用未授权 IPC。

## P0-15 脱敏诊断支持包

- **用户问题**：当前维护者面对“bucket 是空的”一类问题时缺乏足够上下文，只能要求用户继续描述或截图。
- **需求定义**：提供一键导出诊断包，包含版本、平台、连接能力测试、最近错误和请求标识。
- **关键规则**：
  - 默认删除 Token、License、完整域名、完整 bucket 名和对象 key；
  - 用户导出前可以预览内容；
  - 日志有大小和保留期限；
  - Debug 模式不能永久开启或记录文件内容。
- **验收标准**：使用诊断包可以判断常见认证、权限、网络和 API 失败，同时人工扫描确认不含 Secret。

## P0-16 URL 与对象 key 正确编码

- **用户问题**：对象 key 可以包含空格、中文、`#`、`?`、`&` 等字符。直接字符串拼接会造成错误路径、错误查询参数或操作到错误对象。
- **需求定义**：集中实现 bucket、key、prefix 和 query 参数编码，不允许各组件自行拼接。
- **关键规则**：
  - 保留 key 中作为目录语义的 `/`，但逐 segment 编码；
  - prefix、cursor 等使用标准 URLSearchParams；
  - 展示名称与传输 key 分离；
  - Windows 与 macOS 本地文件名差异要有兼容策略。
- **验收标准**：对中文、emoji、空格、百分号、井号、问号、与号、深层路径和超长名称完成全链路测试。

## P0-17 依赖安全治理

- **用户问题**：当前生产依赖审计存在多个 high/moderate advisory，但没有脚本、CI 或例外记录说明哪些会影响桌面产品。
- **需求定义**：建立可持续的依赖升级和漏洞分级机制，而不是一次性执行 `npm audit fix --force`。
- **关键规则**：
  - 区分构建期、主进程、renderer 和不可达依赖；
  - 每个高危项记录适用性、缓解措施和升级计划；
  - 锁文件变更必须经过构建与核心流程回归；
  - CI 阻止新增未评估的高危 advisory。
- **验收标准**：仓库有可重复的 audit 命令、风险基线和升级负责人；不存在“已知但无人解释”的生产高危项。

# P1：完成日常素材工作流

## P1-01 全局传输中心

- **用户问题**：上传对话框关闭后，用户失去任务视图；多个上传和下载同时进行时，不知道哪些已完成或失败。
- **需求定义**：提供常驻的 Transfer Center，统一管理上传、下载、复制和移动任务。
- **关键规则**：显示总体和单项进度、速度、剩余时间、目的路径、状态；支持暂停、取消、重试和清除历史；关闭弹窗不取消任务。
- **验收标准**：应用内任意页面都能查看任务；应用重启后能识别未完成任务并给出恢复或清理选择。

## P1-02 页面级拖拽上传

- **用户问题**：用户预期像文件管理器一样把文件直接拖进当前目录，而不是先寻找 Upload 按钮再拖进弹窗。
- **需求定义**：整个对象区域成为明确的拖放目标，并显示最终 bucket 与 prefix。
- **关键规则**：拖到空白处上传到当前目录；拖到文件夹上传到该目录；拖拽过程中显示目标路径和文件数量；不能遮挡正常滚动和选择。
- **验收标准**：根目录、深层目录、空目录和文件夹卡片四种目标均可准确上传，放手前用户能确认目的地。

## P1-03 文件夹递归上传

- **用户问题**：网站素材、相册和构建产物通常以目录组织，逐文件上传会破坏结构并产生大量操作。
- **需求定义**：支持选择或拖入本地文件夹，保留相对路径递归上传。
- **关键规则**：上传前显示文件总数、总体积和目标 prefix；隐藏文件、符号链接和空目录有明确策略；冲突规则可应用到全部。
- **验收标准**：包含多层目录、空目录、同名文件和特殊字符的样本上传后，远端结构与预览一致。

## P1-04 多选与批量操作

- **用户问题**：清理、迁移和分享多个对象时，目前必须逐个右键，效率极低。
- **需求定义**：支持 Cmd/Ctrl 点击、Shift 连选、框选和全选，并提供批量操作栏。
- **关键规则**：操作栏显示已选数量与总体积；跨分页/加载更多时说明选择范围；危险动作不能因滚动丢失上下文；部分失败逐项列出。
- **验收标准**：键盘和鼠标都能完成选择；1000 个以上对象选择时界面仍可响应；批量结果与远端逐项一致。

## P1-05 完整文件夹操作

- **用户问题**：当前文件夹主要用于导航，创建依赖 `default.txt`；用户还不能自然地重命名、移动、复制或删除整棵目录。
- **需求定义**：把 prefix 作为用户可理解的文件夹实体，提供创建、重命名、移动、复制和删除。
- **关键规则**：对象存储没有真实目录，UI 必须在操作前说明影响对象数；内部占位对象默认隐藏；大目录操作进入后台任务；失败可继续。
- **验收标准**：空文件夹、仅子目录、数千对象目录和跨 bucket 目录操作均有明确、可恢复结果。

## P1-06 移动与复制对象

- **用户问题**：用户需要把对象整理到其他目录或 bucket，而当前只能通过重命名间接完成单文件操作。
- **需求定义**：提供目标选择器和 Move/Copy 两种用户动作，底层复用 P0-07 的可靠事务能力。
- **关键规则**：目标冲突可以跳过、覆盖或自动改名；跨 bucket 时显示权限要求和预估流量；移动只在目标验证后删除来源。
- **验收标准**：单个、批量、同 bucket、跨 bucket 四种情况通过；失败不会丢失来源，部分成功可继续处理。

## P1-07 排序与筛选

- **用户问题**：素材一多，固定的 API 返回顺序无法帮助用户找到最新、最大或特定类型的对象。
- **需求定义**：支持按名称、修改时间、大小和类型排序，并按类型、日期范围、大小范围筛选。
- **关键规则**：明确排序发生在已加载数据还是完整远端结果；条件在目录切换后是否保留可配置；筛选状态必须可见且可一键清除。
- **验收标准**：排序结果稳定；分页加载不会导致前后顺序混乱；筛选后显示命中数量和当前作用域。

## P1-08 可理解的搜索

- **用户问题**：当前是 prefix 搜索，但界面只写 Search，用户可能误以为支持任意位置或内容搜索。
- **需求定义**：明确提供“当前目录前缀搜索”，并根据索引能力增加“整个 bucket 按名称搜索”。
- **关键规则**：显示搜索范围、匹配规则和结果数量；支持最近搜索；请求竞态时只显示最新查询结果；错误不显示成零结果。
- **验收标准**：连续快速输入、切换目录、特殊字符和大 bucket 搜索结果正确；无结果与请求失败有不同状态。

## P1-09 列表与网格双视图

- **用户问题**：Masonry 适合图片，但不适合大量文档、日志、压缩包或需要比较元数据的任务。
- **需求定义**：提供 Grid 和 List 两种视图，并根据文件类型允许用户选择默认值。
- **关键规则**：列表显示名称、大小、类型、修改时间和访问状态；两种视图共享选择和操作状态；窗口缩放时保持可用。
- **验收标准**：切换视图不重新丢失选择、搜索或滚动上下文；偏好按 Profile 或 bucket 保存。

## P1-10 统一预览面板

- **用户问题**：当前主要预览图片，视频渲染代码没有真正进入调用路径，其他对象只能看到图标。
- **需求定义**：提供统一详情/预览面板，支持图片、视频、音频、PDF、文本和 JSON。
- **关键规则**：大文件只加载必要范围；文本有大小上限和编码检测；不可信 HTML/SVG 不直接执行；不支持类型提供下载和系统打开。
- **验收标准**：私有 bucket 中各类型均能预览；危险内容不会执行脚本；加载失败显示类型、原因和替代操作。

## P1-11 对象详情与元数据

- **用户问题**：用户目前很难核对对象完整 key、大小、修改时间、ETag、Content-Type 和缓存元数据。
- **需求定义**：为每个对象提供详情侧栏，并允许受控编辑常用 HTTP metadata。
- **关键规则**：展示值来自远端；修改 Content-Type、Cache-Control、Content-Disposition 前显示影响；完整 key 可复制但不会被视觉截断。
- **验收标准**：详情与 HEAD 结果一致；修改元数据后重新读取验证；权限不足时保留只读详情。

## P1-12 面包屑与导航历史

- **用户问题**：深层目录只有返回按钮和 `root/path` 文本，回到祖先目录、刚访问的位置或另一个 bucket 都不够快。
- **需求定义**：提供可点击面包屑、前进/后退、最近访问和收藏路径。
- **关键规则**：浏览历史保留搜索和滚动位置；当前 bucket 与 Profile 始终可见；不存在的历史路径给出恢复入口。
- **验收标准**：用户可以一键跳到任意祖先目录；返回后保持之前浏览上下文；重启后可访问最近位置。

## P1-13 永久与限时分享

- **用户问题**：并非所有对象都应该永久公开。用户需要临时发文件，但当前分享能力几乎完全依赖公共域名。
- **需求定义**：分享面板明确提供 Public URL 和 Presigned Temporary URL，并允许设置有效期。
- **关键规则**：临时链接使用 S3 endpoint，不假装支持自定义域名；显示 1 秒至 7 天的有效范围；把链接视为 bearer token；复制前显示到期时间。
- **验收标准**：私有对象可生成限时 GET 链接；过期后不可访问；生成分享不会改变 bucket 公开状态。

## P1-14 分享格式模板

- **用户问题**：开发者和内容创作者经常需要 Markdown、HTML、CSS 或 JSON，而不是只复制裸 URL。
- **需求定义**：允许从同一个分享结果生成多种文本模板。
- **关键规则**：模板可插入 URL、文件名、尺寸和 alt；用户可预览最终内容；临时链接模板要带到期提示；自定义模板存储在 Profile 中。
- **验收标准**：内置模板输出语法正确；批量对象保持选择顺序；复制结果与预览完全一致。

## P1-15 自定义域名管理

- **用户问题**：当前只读取第一个 enabled custom domain，用户无法选择默认域名，也不知道 SSL、ownership 或 enable 状态为何不可用。
- **需求定义**：显示所有域名及状态，支持选择默认分享域名，并提供受控的刷新、启用、停用和跳转管理入口。
- **关键规则**：域名初始化、错误、停用分别显示；自定义域名和 r2.dev 独立；改变公开状态必须确认；不能把未 active 域名用于生成链接。
- **验收标准**：多域名 bucket 能选择默认项；状态变化后刷新正确；域名不可用时自动阻止生成错误链接。

## P1-16 多账号与连接配置

- **用户问题**：用户可能同时管理个人、工作和客户 R2；当前只有一套全局 Account ID/Token。
- **需求定义**：提供命名 Profile，每个 Profile 保存独立凭据、默认 bucket、域名偏好和最近路径。
- **关键规则**：切换前显示当前未完成传输；Profile 名称不能代替真实账号标识；删除 Profile 默认只删本地配置，不影响 Cloudflare 资源。
- **验收标准**：至少三个 Profile 可安全切换；Secret 不串用；每个 Profile 的历史和偏好保持隔离。

## P1-17 中英文国际化

- **用户问题**：真实中文用户已经反馈找不到退出 bucket 的入口，并要求国际化。语言障碍会放大权限和删除风险。
- **需求定义**：把界面文本、错误、确认、教程和更新说明统一纳入 i18n，首批支持简体中文和英文。
- **关键规则**：技术名词如 bucket、object、prefix 保留一致翻译策略；不要只翻按钮而留下英文错误；日期、数字和文件大小本地化。
- **验收标准**：两种语言都能完成首次连接到删除对象的完整流程；无硬编码核心文案；长文本不会破坏布局。

## P1-18 键盘与无障碍

- **用户问题**：高频素材管理依赖效率；同时当前部分操作只在 hover 或右键中出现，键盘和辅助技术难以发现。
- **需求定义**：建立完整的键盘导航、快捷键说明和无障碍语义。
- **关键规则**：所有右键动作都有可见替代入口；焦点顺序稳定；危险快捷键需确认；支持 Reduce Motion；图标按钮有可读名称。
- **验收标准**：不使用鼠标也能连接、导航、选择、上传、复制和删除；通过基础屏幕阅读器与对比度检查。

## P1-19 同步与刷新状态

- **用户问题**：R2 可能同时被 Dashboard、CLI 或其他应用修改。用户不知道当前列表来自实时 API 还是本地缓存，也不知道刷新是否成功。
- **需求定义**：显示上次成功同步时间、正在刷新、数据可能过期和远端发生变化等状态。
- **关键规则**：缓存只优化加载，不能覆盖服务端真相；刷新失败保留旧内容并明显标注；乐观更新最终必须远端复核。
- **验收标准**：外部新增、删除、重命名对象后，手动刷新可准确同步；刷新失败不会清空已有列表。

## P1-20 本地小文件缓存

- **用户问题**：重复预览小图片或文本会产生延迟；README 也提出自动下载小对象作为误删缓冲。
- **需求定义**：建立有容量上限、可清理、可解释的本地缓存，并允许用户选择重要前缀离线保留。
- **关键规则**：缓存与备份必须明确区分；私有内容默认受系统用户权限保护；提供容量、路径、清理和禁用设置；使用 ETag/修改时间失效。
- **验收标准**：离线时可访问已缓存对象；远端更新后不会长期显示旧版本；一键清理后磁盘占用可验证下降。

# P2：产品差异化

## P2-01 图片处理预设

- **用户问题**：用户上传网站和博客图片时，通常还要先压缩、缩放或转换格式，工作流被多个工具切开。
- **需求定义**：上传前提供可选的图片压缩、尺寸限制和 WebP/AVIF 转换预设。
- **关键规则**：显示原始与处理后体积、尺寸和画质预览；默认不覆盖本地原图；保留透明度和方向信息；处理失败可上传原文件。
- **验收标准**：常见 JPEG、PNG、WebP 输入结果可预测；用户明确知道最终 key、格式和节省体积。

## P2-02 智能命名规则

- **用户问题**：截图常带无意义名称，批量素材的命名和目录规则需要手工维护。
- **需求定义**：支持基于项目、日期、序号、尺寸、原文件名和随机 ID 的命名模板。
- **关键规则**：上传前预览全部结果；冲突自动编号；模板不会生成非法或空 key；可对单个文件覆写。
- **验收标准**：批量 100 个文件生成唯一、稳定、可预览的 key；取消后不改变本地文件。

## P2-03 重复文件检测

- **用户问题**：同一素材可能以不同名称反复上传，浪费存储，也让用户不知道应该分享哪一个。
- **需求定义**：利用本地 checksum、对象 ETag 和可选索引识别完全相同或高度可能相同的文件。
- **关键规则**：区分内容重复与名称冲突；multipart ETag 不能简单当作文件 MD5；只做提示，不未经确认删除；显示已有对象位置。
- **验收标准**：相同内容不同名称可以识别；不同内容同名称进入冲突流程；误报可以继续上传。

## P2-04 素材属性索引

- **用户问题**：对象数量增大后，只按 key 搜索不足以找到横图、特定尺寸图片或某个时长的视频。
- **需求定义**：在本地维护可重建的素材索引，包括图片尺寸、方向、视频时长、音频时长和基础类型。
- **关键规则**：索引不是远端真相；丢失后可重建；不默认上传私有元数据到第三方；索引任务不阻塞浏览。
- **验收标准**：可按尺寸、方向和时长筛选；删除或修改远端对象后索引能被校正。

## P2-05 使用量与操作指标

- **用户问题**：用户不知道 bucket 有多少对象、占用多少容量、最近请求是否异常增长。
- **需求定义**：读取 Cloudflare GraphQL Analytics API，展示对象数、容量、上传量和操作趋势。
- **关键规则**：显示统计时间范围和数据延迟；与实时列表数量分开；权限不足时提供说明；不伪造即时精确值。
- **验收标准**：24 小时、7 天和 31 天视图可切换；数值与 Cloudflare Dashboard 同一数据源基本一致。

## P2-06 免费额度与成本提示

- **用户问题**：用户担心意外产生费用，但当前 backlog 只有模糊的 free limit 提示，没有数据来源和计算规则。
- **需求定义**：基于当前用量、操作类型和官方定价给出额度进度与成本估算。
- **关键规则**：明确这是估算而不是账单；价格规则带更新时间；区分 Class A、Class B、存储和出口；异常增长比单一阈值更值得提醒。
- **验收标准**：用户能看到计算依据；定价配置可以更新；无数据时不显示误导性的零成本。

## P2-07 本地目录同步

- **用户问题**：长期维护同一网站或素材库时，重复手动选择文件很低效。
- **需求定义**：先提供本地到 R2 的单向增量同步，监听指定目录并生成变更计划。
- **关键规则**：默认不把本地删除同步为远端永久删除；支持忽略规则；同步前预览新增、更新、跳过和冲突；可暂停。
- **验收标准**：首次全量和后续增量结果一致；应用重启、文件快速变化和网络中断不会产生重复或丢失。

## P2-08 备份任务

- **用户问题**：用户希望重要素材有第二份副本，而普通缓存不能承担长期恢复责任。
- **需求定义**：支持把指定 bucket/prefix 定期备份到本地目录或另一个 bucket。
- **关键规则**：备份有清单、校验和保留策略；删除源对象不会立即删除备份；恢复可以选择原路径或新路径；任务失败告警。
- **验收标准**：随机抽样和完整清单校验通过；可以从备份恢复单个对象和整个 prefix。

## P2-09 Lifecycle 管理

- **用户问题**：临时文件、日志和未完成 multipart 需要自动清理，但非技术用户难以直接配置规则。
- **需求定义**：用场景化界面创建、查看和修改 R2 Lifecycle Rules。
- **关键规则**：保存前显示受影响 prefix、预计匹配对象和时间；高风险规则二次确认；保留原始 JSON/配置详情供高级用户核对。
- **验收标准**：创建后从 Cloudflare 重新读取并显示；修改和删除规则都有审计记录；不会把预览当作精确未来结果。

## P2-10 Cache-Control 与 CORS 预设

- **用户问题**：网站素材需要正确缓存和跨域配置，但普通用户不理解 header 与规则组合。
- **需求定义**：提供“长期静态素材”“频繁更新文件”“浏览器直传”等场景预设。
- **关键规则**：显示预设实际会修改的 header/CORS；允许高级编辑；修改 bucket 级设置前说明影响范围；支持导出和回滚配置。
- **验收标准**：使用预设后通过真实请求检查 header 和浏览器跨域行为；回滚能恢复原配置。

## P2-11 CDN 与域名健康检查

- **用户问题**：分享链接打不开时，问题可能来自域名 ownership、SSL、DNS、公开访问、WAF 或缓存，用户无法定位。
- **需求定义**：提供分层健康检查和清晰诊断，而不是只有“链接失败”。
- **关键规则**：分别检查 Cloudflare 配置状态、DNS、TLS、HTTP status 和缓存 header；不把客户端网络问题误判为域名故障；提供 Dashboard 跳转。
- **验收标准**：对初始化中、SSL 错误、域名停用、404、403 和网络离线给出不同结果。

## P2-12 事件驱动刷新

- **用户问题**：多人或多个工具同时操作 R2 时，轮询和手动刷新会让本地列表滞后。
- **需求定义**：可选接入 R2 Event Notifications，让对象新增和删除触发本地索引刷新。
- **关键规则**：这是增强能力，不能成为基础浏览依赖；事件可能重复或乱序，需要幂等处理；配置 Queue 的复杂度要对用户透明或有明确引导。
- **验收标准**：外部上传和删除能在合理时间内反映；漏事件后通过完整刷新可以自愈。

## P2-13 快速上传器

- **用户问题**：高频用户希望截图后不打开主窗口，就能上传并拿到链接。
- **需求定义**：提供菜单栏、托盘或全局快捷键入口，支持剪贴板图片和最近文件快速上传。
- **关键规则**：操作前或通知中明确当前 Profile、bucket 和 prefix；目标不可用时不能静默传到其他位置；失败进入传输中心。
- **验收标准**：从截图到剪贴板获得链接可在一次动作内完成；误触可取消；目标始终可见。

## P2-14 批量链接清单

- **用户问题**：批量上传后，用户需要一次性获得所有结果，而不是逐个点击复制。
- **需求定义**：从上传任务或多选对象生成 URL、Markdown、HTML、JSON 或 CSV 清单。
- **关键规则**：保持用户选择或上传顺序；失败项不会生成假链接；临时链接清单显示共同或各自过期时间；支持文件名映射。
- **验收标准**：100 个对象可一次复制或导出；清单数量、顺序和远端成功项完全一致。

## P2-15 操作历史

- **用户问题**：用户需要知道自己刚才上传、移动、改名或删除了什么，并从错误操作中恢复。
- **需求定义**：建立本地操作历史，记录动作、时间、Profile、目标和结果。
- **关键规则**：历史默认脱敏；不是 Cloudflare 账号级审计；可恢复操作显示 Undo/Restore；失败和部分成功保留详情；有清理期限。
- **验收标准**：核心 mutation 都进入历史；从历史能定位对象并执行允许的恢复；清除历史不会改变远端资源。

# P3：个人效率与生态扩张

团队工作区、成员管理、RBAC 和团队审计已经明确移出正式路线图。P3 只保留能够增强个人用户效率或扩大个人工具生态的能力。

## P3-01 加密设置同步

- **用户问题**：用户换电脑后希望恢复 Profile、收藏、命名预设和任务配置，但这些信息可能包含敏感上下文。
- **需求定义**：提供端到端加密的配置同步，并设计恢复密钥或设备授权流程。
- **关键规则**：服务端无法读取明文；Secret 是否同步要单独决定；冲突可见；用户可以只同步非敏感偏好。
- **验收标准**：新设备可以恢复所选配置；丢失恢复密钥的后果提前说明；撤销设备后无法继续解密新数据。

## P3-02 自动化规则

- **用户问题**：重复上传流程可能包含固定的命名、优化、目录、备份和通知步骤。
- **需求定义**：允许用户创建“触发条件 → 条件判断 → 动作”的自动化规则。
- **关键规则**：规则上线前可模拟；危险动作默认关闭；循环触发检测；每次运行进入历史；失败可重试或人工介入。
- **验收标准**：同一事件不会重复执行非幂等动作；模拟结果与实际执行一致；用户可快速停用全部规则。

## P3-03 CLI、插件与浏览器扩展

- **用户问题**：开发者希望从终端、编辑器或浏览器直接调用已经配置好的上传和分享能力。
- **需求定义**：在桌面核心 API 稳定后，提供受控 CLI、插件协议或本地服务接口。
- **关键规则**：外部客户端不能直接取得长期 Secret；权限和用户确认按调用方隔离；协议版本化；支持撤销客户端授权。
- **验收标准**：第三方只能调用公开、文档化能力；越权调用被拒绝；桌面升级保持兼容或提供迁移说明。

## P3-04 S3 兼容存储扩展

- **用户问题**：部分用户同时使用 R2、MinIO、AWS S3 或其他兼容服务，希望复用同一素材体验。
- **需求定义**：在 R2 产品主线稳定后，把认证对象层抽象为可插拔 provider。
- **关键规则**：不能把“API 大致兼容”当作功能完全一致；每个 provider 声明支持矩阵；R2 特有域名、指标和 Lifecycle 能力单独保留。
- **验收标准**：同一核心用例在目标 provider 的真实环境通过；不支持能力明确禁用，而不是运行后报通用错误。

## P3-05 Web / 移动伴侣

- **用户问题**：用户离开主电脑时可能只需要查找、预览和生成临时分享链接。
- **需求定义**：提供范围受限的 Web 或移动端伴侣，优先只读与安全分享，而不是复制完整桌面应用。
- **关键规则**：需要可靠身份、设备管理和服务端边界；默认不缓存敏感内容；丢失设备可撤销；离线能力谨慎设计。
- **验收标准**：移动端不持有长期全权限 Secret；只读和分享权限可独立授权；撤销后立即失效。

## P3-06 AI 标签与语义搜索

- **用户问题**：当素材规模足够大且文件名质量很差时，用户可能希望按画面内容、主题或自然语言查找。
- **需求定义**：在传统元数据索引被证明不足后，为用户主动选择的素材生成标签或向量索引。
- **关键规则**：默认不把私有素材发送给第三方模型；明确处理位置、费用和删除方式；AI 结果是辅助索引，不改变远端对象；允许纠错。
- **验收标准**：在真实大素材集上证明搜索成功率优于名称/元数据搜索；关闭功能后可以完整删除派生数据。

# 需求之间的关键依赖

```text
P0-03 凭据保险库
  └─ P0-02 认证对象访问层
       ├─ P0-06 认证式查重
       ├─ P0-07 原子化重命名与移动
       ├─ P0-08 可靠上传引擎
       ├─ P0-09 安全删除与恢复
       └─ P1-13 永久与限时分享

P0-05 统一错误模型
  ├─ P0-04 连接诊断
  ├─ P1-01 传输中心
  └─ P0-15 诊断支持包

P0-12 发布闭环
  └─ 所有面向用户的修复才能真正到达用户

P1-19 同步状态 + P2-04 素材索引
  ├─ P2-03 重复检测
  ├─ P2-12 事件驱动刷新
  └─ P3-06 语义搜索
```

# 推荐的需求切分方式

不要把每个编号直接等同于一个开发 Ticket。更合理的做法是：

1. 先把一个需求写成用户可验收的 Epic；
2. 再拆成 main process、renderer、迁移、测试、文档和发布子任务；
3. P0 需求必须包含故障注入或安全回归；
4. P1/P2 需求必须同时定义空状态、加载状态、失败状态和权限不足状态；
5. 只有官方 Release 被真实用户安装后，需求才算最终交付。

最先应该进入实施设计的组合是：

- P0-01 Private-first bucket；
- P0-02 认证对象访问层；
- P0-03 系统安全凭据保险库；
- P0-04 连接测试与权限诊断；
- P0-05 统一错误模型。

这五项共同决定产品能否从“依赖公开链接的便利工具”转变成“可信任的 R2 客户端”。
